import 'package:flutter/material.dart';

class KRSPage extends StatelessWidget {
  const KRSPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Halaman KRS"),
        backgroundColor: Colors.yellow,
      ),
    );
  }
}
